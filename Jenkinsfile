pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'i am ready to join ok it happen ....'
            }
        }
        stage('Working with') {  // Changed the name to 'Working with'
            steps {
              //  sh  'mkdir working'
                sh 'ls -la'
            }
        }
    }
}
